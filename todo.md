*Website Todos

* figure out why footer is a tiny bit longer than everything else
* find best practices for how to style nav text. should I be using headers?
* add social buttons, perhaps use a :hover to make them float in under my big head
* better titles, link names
* link back to main page AND blog- find a nice UX way to do this while maintaining continuity across site.
* solve the mystery of the two floating header divs. make them sit next to each other nicely with text wrapping.
* revisit blog post formatting. its a bit plain.
* nav is different in blog and main page esp link/text styling. figure out which is best esp in terms of text alignment. i've had to do a lot of styling on the header elements to make them squeeze into the nav on the main page. alex knoll's site puts the header tags outside like h3 - a href="#" - /a - /h3 but when i did this it created problems with my existing styling.
* change css to use background-color on body rather than the "article" or whatever section of a page.
